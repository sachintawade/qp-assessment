mkdir groceryApp && cd groceryApp

npm init --yes

npx tsc --init

create folder structure and changes in tsconfig.json file i.e set rootDir and outDir also include and exclude parameters

then

npm i express bcrypt jsonwebtoken
npm i --save-dev typescript @types/express @types/express ts-node

created Dockerfile and added steps
created docker-compose.yml and added data

npm install db-migrate db-migrate-mysql

npm i sequelize sequelize-cli

npm i --save-dev @types/bcrypt